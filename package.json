{
  "name": "blog",
  "version": "1.0.0",
  "description": "A blog for metadelta.",
  "main": "build.js",
  "scripts": {
    "start": "node build.js",
    "build": "npm start",
    "test": "node build.js && open index.html",
    "deploy": "node build && surge ."
  },
  "author": "Gerald Nash",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^0.22.0"
  }
}
